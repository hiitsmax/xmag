# xmag

Convert a list of X article URLs into a three-column LaTeX-style PDF.

This project is implemented with:

- Playwright for content extraction.
- Jinja2 + LaTeX for rendering.
- Tectonic for deterministic PDF compilation.

See `docs/codex/` for implementation notes and testing guidance.
